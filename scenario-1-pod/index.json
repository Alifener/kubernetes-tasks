{
  "title": "Pod Scenario",
  "description": "Learn how to create a basic Pod in Kubernetes with a single container.",
  "duration": 10,
  "tasks": [
    {
      "description": "Create a Pod named 'busybox-pod' with the preloaded busybox image.",
      "command": "kubectl run busybox-pod --image=busybox -- sleep 3600"
    },
    {
      "description": "Verify the Pod is running.",
      "command": "kubectl get pods"
    },
    {
      "description": "Delete the Pod when done.",
      "command": "kubectl delete pod busybox-pod"
    }
  ],
  "backend": {
    "imageid": "kubernetes-kubeadm-1node"
   }
}
